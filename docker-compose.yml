services:
  postgres:
    image: postgres:17-alpine
    container_name: groupchat-postgres
    restart: unless-stopped
    environment:
      POSTGRES_USER: ${POSTGRES_USER:-groupchat}
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-groupchat}
      POSTGRES_DB: ${POSTGRES_DB:-groupchat}
    ports:
      - '127.0.0.1:${POSTGRES_PORT:-10102}:5432'
    volumes:
      - postgres-data:/var/lib/postgresql/data
    healthcheck:
      test: ['CMD-SHELL', 'pg_isready -U ${POSTGRES_USER:-groupchat} -d ${POSTGRES_DB:-groupchat}']
      interval: 5s
      timeout: 5s
      retries: 10

volumes:
  postgres-data:
